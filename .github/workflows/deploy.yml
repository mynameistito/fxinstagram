name: Deploy

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
  pull_request_target:
    types: [opened, reopened, synchronize]
  pull_request:
    types: [closed]

jobs:
  deploy:
    uses: mynameistito/alchemy-deploy/.github/workflows/deploy.yml@2615f57b12c33ef256d8395a559f5f3643843beb # v1.0.0
    permissions:
      actions: read
      contents: read
      deployments: write
      pull-requests: write
    with:
      worker-name: fxinstagram
      deploy-command: bunx --no-install alchemy deploy alchemy.run.ts --stage "$STAGE" --yes
      destroy-command: bunx --no-install alchemy destroy alchemy.run.ts --stage "$STAGE" --yes
      production-stage: prod
      production-url: https://ig.mynameistito.com
      use-adopt: false
    secrets:
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
