# Required by the local runtime and worker configuration.
PUBLIC_ORIGIN=https://fxinstagram.example/
METADATA_TIMEOUT_MS=1000
METADATA_CACHE_TTL_SECONDS=60
ALLOWED_MEDIA_HOSTS=cdn.example

# Optional provider credential. It is redacted at the configuration boundary.
METADATA_PROVIDER_TOKEN=
